<!DOCTYPE html>
<html>
  <head>
    <title>AudioPlayer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/amplitudejs@5.3.2/dist/amplitude.js"></script>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#F8FAFC] dark:bg-[#0A1122]">
    <%= render partial: "shared/player_audio" unless ["users", "sessions", "passwords", "musics"].include?(controller_name) %>
    <%= render partial: "shared/navbar" unless ["users", "sessions", "passwords"].include?(controller_name)%>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
